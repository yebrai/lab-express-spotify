{{#each artistAlbum}}
  <div>
    {{#each images}}
            {{#if @last}}
        <img src="{{url}}" />
                {{/if}}
    {{/each}}
    <p>{{name}}</p>
    <a href="/tracks/{{id}}">View Albums</a>
  </div>
{{/each}}